// Locale helpers — delegates live in main.dart via MyApp.setLocale()
//
// Usage: MyApp.setLocale(context, const Locale('ar'));
//        MyApp.setLocale(context, const Locale('en'));
//
// This file documents the locale-switching contract so future contributors
// know where to look without searching the whole codebase.
export '../../app_localizations.dart' show AppLocalizations;
export 'package:flutter/material.dart' show Locale;
