// ---------------------------------------------------------------------------
// Shared in-memory auth state — imported by main.dart and login_page.dart
// ---------------------------------------------------------------------------
class AuthState {
  static bool isLoggedIn = false;
  static String? userToken;
}
